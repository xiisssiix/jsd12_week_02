// Mongodb เป็น Document-based Database รองรับ NoSQL
// ใน mongodb เราจะไม่เก็บข้อมูลเป็นตาราง แต่เราจะเก็บเป็นรูปแบบเอกสาร
// เช่น (จะเห็นได้ว่า มันอ่านออกเลย ว่าข้อมูลคืออะไร key จะมากน้อยไม่เท่ากันก็ได้ แล้วก็ จะเพิ่มข้อมูลมาทีหลังก็ได้ ในขณะที่ SQL ทั่วไปก็ทำได้ แต่ยิ่งข้อมูลที่เพิ่มมา มีมากขึ้น ตารางก็จะมากขึ้น และอาจเกิดความซับซ้อนขึ้นมาทีหลังได้)

{
  "_id": "64a1b2c3d4e5f67890123456",
  "username": "mobilesuit",
  "email": "mobilesuit@example.com",
  "hobbies": ["coding", "gaming"]
}

// mongodb ยังมี Aggregation Pipeline ที่ทำให้เราสามารถวิเคราะห์หรือเรียกข้อมูลมาได้เร็วกว่า SQL ด้วย
